this code is created to calculate multiple things from a csv file. the first row should be the headers of each collumn which 
are timestamp, phase a, phase b, phase c, line current, frequenct, power factor, thd percent the next 1000 rows should be readings
each the record fr function records this data so should always be done first
each other function is an eqation that runs down a specific column, type which column (0-7) in the functions bracket eg rms(1) 
to work out the rms of phase a.
the code will output the answer of each equation in the window.
